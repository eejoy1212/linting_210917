import 'package:flutter_test/flutter_test.dart';

void main() {
  Map<int, List<Person>> groupByZip(Iterable<Person> people) {
    var groupByZip = <int, List<Person>>{};
    groupByZip ="String";
    for (Person person in people) {
      peopleByZip.putIfAbsent
    }
  }

  testWidgets('unnecessary_this', (WidgetTester tester) async{
    await tester.pumpWidget(UnnecessaryThisWidget());
  });

  test('unnecessary_new', (){
    var person = Person();
    var person1 = new Person();
  });

  test('curly_braces_in_flow_control_structures', (){
    var a = 'C';
    if(a=='A')
      print('A입니다.');
    else if(a=='B')
      print('B입니다.');
    else if(a=='C')print('C입니다.');
  });

}

class Person {
}

